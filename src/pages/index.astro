---
import BaseLayout from '../layouts/BaseLayout.astro';
import { withBase } from '../lib/paths';
import { heroMessaging, proofScreenshots, trustChips } from '../content/marketingData';
---
<style>
/* ── HOME-SPECIFIC ── */
.hero-light { padding: 80px 0 0; overflow: hidden; }
.hero-inner {
  max-width: 1160px;
  margin: 0 auto;
  padding: 0 24px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 64px;
  align-items: start;
}
.hero-content { padding-bottom: 80px; }
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: rgba(74,144,217,0.1);
  color: var(--primary);
  font-size: 12px;
  font-weight: 600;
  padding: 5px 14px;
  border-radius: 100px;
  margin-bottom: 20px;
  border: 1px solid rgba(74,144,217,0.2);
  letter-spacing: 0.5px;
  text-transform: uppercase;
}
.hero h1 {
  font-size: clamp(36px, 4.5vw, 54px);
  font-weight: 900;
  line-height: 1.08;
  letter-spacing: -0.03em;
  color: var(--primary);
  margin-bottom: 20px;
}
.hero h1 em { font-style: italic; color: var(--secondary); }
.hero-sub { font-size: 18px; font-weight: 500; color: var(--gray-700); line-height: 1.5; margin-bottom: 14px; }
.hero-body { font-size: 15px; color: var(--gray-700); line-height: 1.75; margin-bottom: 32px; max-width: 480px; }
.hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 36px; }
.social-proof { display: flex; align-items: center; gap: 10px; font-size: 13px; color: var(--gray-700); }
.social-proof-avatars { display: flex; }
.social-proof-avatars span {
  width: 30px; height: 30px;
  border-radius: 50%;
  border: 2px solid white;
  display: flex; align-items: center; justify-content: center;
  font-size: 11px; font-weight: 700;
  margin-left: -8px; color: white;
}
.social-proof-avatars span:first-child { margin-left: 0; }
.av1 { background: #245F99; }
.av2 { background: #047857; }
.av3 { background: #B45309; }
.av4 { background: #7C3AED; }
.hero-mockup-wrap { position: relative; padding-top: 20px; }
.hero-mockup-frame {
  border-radius: 0 0 12px 12px;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(30,58,95,0.18), 0 4px 16px rgba(30,58,95,0.1);
}
.hero-mockup-shot {
  width: 100%;
  height: auto;
  aspect-ratio: 7 / 4;
  display: block;
  object-fit: cover;
  object-position: center;
  background: var(--primary);
}
.browser-chrome {
  background: #E8ECEF;
  border-radius: 12px 12px 0 0;
  padding: 10px 14px 0;
  display: flex; align-items: center; gap: 8px;
}
.chrome-dots { display: flex; gap: 5px; }
.chrome-dots span { width: 11px; height: 11px; border-radius: 50%; }
.dot-red { background: #FF5F57; }
.dot-yellow { background: #FEBC2E; }
.dot-green { background: #28C840; }
.chrome-bar {
  flex: 1; background: white; border-radius: 5px; height: 22px;
  margin-left: 8px; display: flex; align-items: center;
  padding: 0 10px; font-size: 11px; color: var(--gray-500); gap: 5px;
}
.chrome-bar::before { content: '🔒'; font-size: 9px; }
.app-window {
  background: #F1F4F8;
  border-radius: 0 0 12px 12px;
  overflow: hidden;
  display: flex;
  box-shadow: 0 20px 60px rgba(30,58,95,0.18), 0 4px 16px rgba(30,58,95,0.1);
  min-height: 380px;
}
.app-sidebar { width: 180px; background: var(--primary); flex-shrink: 0; padding: 16px 0; }
.sidebar-logo { padding: 0 16px 16px; font-size: 15px; font-weight: 800; color: white; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 8px; }
.sidebar-nav a {
  display: flex; align-items: center; gap: 8px;
  padding: 8px 16px; font-size: 12px;
  color: rgba(255,255,255,0.6); text-decoration: none;
  border-radius: 6px; margin: 0 6px; transition: background 0.15s;
  font-weight: 500;
}
.sidebar-nav a.active { background: rgba(255,255,255,0.12); color: white; }
.app-main { flex: 1; padding: 20px; display: flex; flex-direction: column; gap: 14px; background: var(--off-white); }
.app-title { font-size: 15px; font-weight: 700; color: var(--primary); }
.app-sub { font-size: 11px; color: var(--gray-500); }
.ai-badge {
  display: inline-flex; align-items: center; gap: 5px;
  background: linear-gradient(135deg, #7C3AED, #4A90D9);
  color: white; font-size: 10px; font-weight: 700;
  padding: 4px 10px; border-radius: 100px;
}
.scorecard-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
.score-card { background: white; border-radius: 8px; padding: 12px 14px; border: 1px solid var(--gray-100); }
.score-label { font-size: 10px; color: var(--gray-500); font-weight: 500; margin-bottom: 4px; text-transform: uppercase; letter-spacing: 0.5px; }
.score-value { font-size: 24px; font-weight: 800; line-height: 1; }
.score-unit { font-size: 12px; font-weight: 500; color: var(--gray-500); }
.score-trend { font-size: 10px; font-weight: 600; margin-top: 3px; }
.score-blue .score-value { color: var(--secondary); }
.score-blue .score-trend { color: var(--secondary); }
.score-green .score-value { color: var(--success); }
.score-green .score-trend { color: var(--success); }
.score-orange .score-value { color: var(--accent); }
.score-orange .score-trend { color: var(--accent); }
.score-red .score-value { color: var(--danger); }
.score-red .score-trend { color: var(--danger); }
.app-meeting-card {
  background: white; border-radius: 8px; padding: 12px 14px;
  border: 1px solid var(--gray-100);
  display: flex; justify-content: space-between; align-items: center;
}
.meeting-info { font-size: 12px; }
.meeting-title { font-weight: 600; color: var(--primary); margin-bottom: 2px; }
.meeting-meta { color: var(--gray-500); }
.meeting-status { font-size: 10px; font-weight: 600; padding: 3px 8px; border-radius: 100px; background: rgba(16,185,129,0.1); color: var(--success); }
.section-problem { background: var(--primary); padding: 88px 0; color: white; }
.problem-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; margin-top: 48px; align-items: start; }
.problem-text { font-size: 16px; color: rgba(255,255,255,0.92); line-height: 1.75; }
.problem-text p + p { margin-top: 16px; }
.failure-cards { display: flex; flex-direction: column; gap: 16px; }
.failure-card {
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 12px; padding: 20px 22px;
  transition: background 0.2s;
}
.failure-card:hover { background: rgba(255,255,255,0.09); }
.failure-icon { width: 36px; height: 36px; border-radius: 8px; background: rgba(245,166,35,0.15); display: flex; align-items: center; justify-content: center; font-size: 16px; margin-bottom: 10px; border: 1px solid rgba(245,166,35,0.25); }
.failure-card h4 { font-size: 14px; font-weight: 700; color: white; margin-bottom: 6px; }
.failure-card p { font-size: 13px; color: rgba(255,255,255,0.88); line-height: 1.6; }
.problem-bridge {
  margin-top: 48px; padding-top: 40px;
  border-top: 1px solid rgba(255,255,255,0.1);
  text-align: center; font-size: 18px; font-weight: 600;
  color: rgba(255,255,255,0.95); max-width: 600px;
  margin-left: auto; margin-right: auto;
}
.problem-bridge strong { color: #F8BB52; }
.pillars { display: grid; grid-template-columns: repeat(3, 1fr); gap: 28px; margin-top: 16px; }
.pillar { background: var(--off-white); border-radius: 16px; padding: 32px; border: 1px solid var(--gray-100); }
.pillar-icon { width: 48px; height: 48px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 22px; margin-bottom: 18px; }
.pillar-icon-1 { background: rgba(16,185,129,0.1); border: 1px solid rgba(16,185,129,0.2); }
.pillar-icon-2 { background: rgba(74,144,217,0.1); border: 1px solid rgba(74,144,217,0.2); }
.pillar-icon-3 { background: #FFFFFF; border: 1px solid var(--gray-300); color: #111827; }
.pillar h3 { font-size: 12px; font-weight: 700; color: var(--gray-700); text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 8px; }
.pillar h4 { font-size: 19px; font-weight: 700; color: var(--primary); margin-bottom: 10px; }
.pillar p { font-size: 14px; color: var(--gray-700); line-height: 1.65; }
.modules-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin-top: 40px; }
.module-tile {
  background: var(--white); border-radius: 14px; padding: 24px;
  border: 1px solid var(--gray-100);
  transition: transform 0.2s, box-shadow 0.2s, border-color 0.2s;
  text-decoration: none; display: block;
}
.module-tile:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(30,58,95,0.1); border-color: var(--secondary); }
.module-emoji { font-size: 26px; margin-bottom: 12px; display: block; }
.module-tile h3 { font-size: 14px; font-weight: 700; color: var(--primary); margin-bottom: 6px; }
.module-tile p { font-size: 12px; color: var(--gray-500); line-height: 1.55; }
.module-link { display: inline-flex; align-items: center; gap: 4px; font-size: 12px; font-weight: 600; color: var(--secondary); margin-top: 10px; }
.personas-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 20px; margin-top: 40px; }
.persona-card { border-radius: 16px; padding: 28px; border: 1px solid var(--gray-100); transition: transform 0.2s, box-shadow 0.2s; text-decoration: none; display: block; }
.persona-card:hover { transform: translateY(-4px); box-shadow: 0 12px 40px rgba(30,58,95,0.1); }
.persona-ic { background: #EFF6FF; border-color: #BFDBFE; }
.persona-mgr { background: #F0FDF4; border-color: #BBF7D0; }
.persona-l2 { background: #FFF7ED; border-color: #FED7AA; }
.persona-chro { background: #F5F3FF; border-color: #DDD6FE; }
.persona-ceo { background: #F8FAFC; border-color: #CBD5E1; }
.persona-avatar { width: 44px; height: 44px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 18px; margin-bottom: 16px; background: white; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
.persona-card h3 { font-size: 15px; font-weight: 700; color: var(--primary); margin-bottom: 8px; }
.persona-card p { font-size: 13px; color: var(--gray-900); line-height: 1.6; }
.pricing-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-top: 40px; }
.pricing-card { border-radius: 16px; border: 1.5px solid var(--gray-200); padding: 32px; background: var(--white); position: relative; transition: all 0.2s; }
.pricing-card:hover { transform: translateY(-4px); box-shadow: 0 12px 40px rgba(30,58,95,0.1); }
.pricing-card.featured { border-color: var(--secondary); box-shadow: 0 0 0 3px rgba(74,144,217,0.1); }
.pricing-badge { position: absolute; top: -12px; left: 50%; transform: translateX(-50%); background: var(--secondary); color: white; font-size: 11px; font-weight: 700; padding: 4px 14px; border-radius: 100px; white-space: nowrap; letter-spacing: 0.5px; text-transform: uppercase; }
.pricing-tier { font-size: 12px; font-weight: 700; color: var(--secondary); text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 10px; }
.pricing-price { font-size: 40px; font-weight: 900; color: var(--primary); letter-spacing: -1.5px; line-height: 1; margin-bottom: 4px; }
.pricing-price span { font-size: 16px; font-weight: 500; color: var(--gray-500); letter-spacing: 0; }
.pricing-meta { font-size: 12px; color: var(--gray-500); margin-bottom: 20px; }
.pricing-desc { font-size: 14px; color: var(--gray-700); line-height: 1.6; margin-bottom: 20px; }
.pricing-features { list-style: none; display: flex; flex-direction: column; gap: 9px; margin-bottom: 24px; }
.pricing-features li { display: flex; gap: 9px; font-size: 13px; color: var(--gray-700); }
.pricing-features li::before { content: '✓'; font-weight: 700; color: var(--success); flex-shrink: 0; }
.btn-pricing { display: block; text-align: center; font-size: 14px; font-weight: 600; padding: 12px; border-radius: 10px; text-decoration: none; transition: all 0.15s; }
.btn-pricing-primary { background: var(--primary); color: white; }
.btn-pricing-primary:hover { background: var(--primary-dark); }
.btn-pricing-outline { background: transparent; color: var(--primary); border: 2px solid var(--gray-200); }
.btn-pricing-outline:hover { border-color: var(--primary); }
@media (max-width: 1024px) {
  .modules-grid { grid-template-columns: repeat(2, 1fr); }
  .personas-grid { grid-template-columns: repeat(2, 1fr); }
  .pillars { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 768px) {
  .hero-inner { grid-template-columns: 1fr; }
  .hero-mockup-wrap { display: none; }
  .problem-grid { grid-template-columns: 1fr; gap: 40px; }
  .pillars { grid-template-columns: 1fr; }
  .pricing-grid { grid-template-columns: 1fr; }
}
@media (max-width: 480px) {
  .modules-grid { grid-template-columns: 1fr; }
  .personas-grid { grid-template-columns: 1fr; }
}

/* HUMAN-IN-THE-LOOP / TRUST SECTION */
.hitl-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  margin-top: 48px;
}
.hitl-card {
  background: white;
  border-radius: 16px;
  padding: 32px;
  border: 1px solid var(--gray-100);
  transition: all 0.2s;
}
.hitl-card:hover { box-shadow: 0 8px 24px rgba(30,58,95,0.08); transform: translateY(-2px); }
.hitl-icon {
  width: 52px; height: 52px;
  border-radius: 14px;
  display: flex; align-items: center; justify-content: center;
  font-size: 24px; margin-bottom: 20px;
}
.hitl-icon-1 { background: rgba(16,185,129,0.1); border: 1px solid rgba(16,185,129,0.2); }
.hitl-icon-2 { background: rgba(74,144,217,0.1); border: 1px solid rgba(74,144,217,0.2); }
.hitl-icon-3 { background: rgba(139,92,246,0.1); border: 1px solid rgba(139,92,246,0.2); }
.hitl-card h3 { font-size: 12px; font-weight: 700; color: var(--gray-500); text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 8px; }
.hitl-card h4 { font-size: 19px; font-weight: 700; color: var(--primary); margin-bottom: 12px; }
.hitl-card p { font-size: 14px; color: var(--gray-500); line-height: 1.7; }
.hitl-card .hitl-quote {
  margin-top: 16px;
  padding: 12px 16px;
  background: var(--off-white);
  border-left: 3px solid var(--secondary);
  border-radius: 0 8px 8px 0;
  font-size: 13px; font-style: italic; color: var(--gray-700); line-height: 1.6;
}
.trust-strip {
  display: flex; gap: 12px; flex-wrap: wrap; justify-content: center;
  margin-top: 48px; padding-top: 36px;
  border-top: 1px solid var(--gray-100);
}
.trust-pill {
  display: inline-flex; align-items: center; gap: 8px;
  background: var(--off-white);
  border: 1px solid var(--gray-200);
  border-radius: 100px; padding: 10px 20px;
  font-size: 13px; font-weight: 600; color: var(--gray-700);
}
.trust-pill a { color: var(--secondary); text-decoration: none; font-weight: 700; }
.trust-pill a:hover { text-decoration: underline; }

.hero-trust-line {
  display: flex; align-items: center; gap: 8px; flex-wrap: wrap;
  font-size: 12px; color: var(--gray-700); margin-top: 14px;
}
.hero-trust-line span.ht-dot {
  display: inline-block; width: 4px; height: 4px;
  border-radius: 50%; background: var(--gray-300);
}
.hero-trust-line .hero-chip {
  display: inline-flex; align-items: center; gap: 6px;
  color: var(--gray-900); font-weight: 700;
}
.hero-trust-line .hero-chip::before {
  content: '✓'; color: #064E3B; font-weight: 900;
}
.proof-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 18px;
  margin-top: 40px;
  width: 100%;
  justify-items: stretch;
  align-items: stretch;
}
.proof-card {
  min-width: 0;
  background: white;
  border: 1px solid var(--gray-100);
  border-radius: 14px;
  overflow: hidden;
  box-shadow: 0 8px 28px rgba(30,58,95,0.08);
}
.proof-card a {
  display: block;
  cursor: zoom-in;
  background: var(--gray-50);
}
.proof-card img {
  width: 100%;
  aspect-ratio: 16 / 10;
  object-fit: cover;
  object-position: top center;
  display: block;
}
.proof-card-body { padding: 16px; }
.proof-card h3 { font-size: 14px; font-weight: 800; color: var(--primary); margin-bottom: 7px; }
.proof-card p { font-size: 12px; line-height: 1.6; color: var(--gray-500); }
.proof-note {
  margin-top: 18px;
  font-size: 12px;
  color: var(--gray-500);
  text-align: center;
}

@media (max-width: 1024px) {
  .proof-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
}

@media (max-width: 768px) {
  .hitl-grid { grid-template-columns: 1fr; }
  .trust-strip { gap: 8px; }
  .proof-grid { grid-template-columns: 1fr; }
}
</style>

<BaseLayout title="Cadence — Management Operating Plane for Distributed Teams" description="Close the remote-contributor management chasm with structured 1:1s, goals, ER, recognition, surveys, and culture signal in one human-owned operating plane.">

<!-- HERO -->
<section class="hero-light" id="hero">
  <div class="hero-inner">
    <div class="hero-content fade-up">
      <div class="hero-badge">{heroMessaging.eyebrow}</div>
      <h1 class="hero">{heroMessaging.headline}</h1>
      <p class="hero-sub">{heroMessaging.subhead}</p>
      <p class="hero-body">{heroMessaging.body}</p>
      <div class="hero-ctas">
        <a href={heroMessaging.primaryCta.href} class="btn btn-primary btn-lg">{heroMessaging.primaryCta.label} →</a>
        <a href={heroMessaging.secondaryCta.href} class="btn btn-secondary btn-lg">{heroMessaging.secondaryCta.label}</a>
      </div>
      <div class="hero-trust-line">
        {trustChips.map((chip, index) => (
          <>
            <span class="hero-chip">{chip}</span>
            {index < trustChips.length - 1 && <span class="ht-dot"></span>}
          </>
        ))}
        <span class="ht-dot"></span>
        <a href={withBase('/security')}>Full trust &amp; privacy details →</a>
      </div>
      <div class="social-proof" style="margin-top:20px;">
        <div class="social-proof-avatars">
          <span class="av1">VP</span>
          <span class="av2">HR</span>
          <span class="av3">C</span>
          <span class="av4">L2</span>
        </div>
        <span>Built for hybrid and remote-first organizations where management quality has to scale.</span>
      </div>
    </div>
    <div class="hero-mockup-wrap fade-up" style="transition-delay:0.15s">
      <div class="browser-chrome">
        <div class="chrome-dots">
          <span class="dot-red"></span>
          <span class="dot-yellow"></span>
          <span class="dot-green"></span>
        </div>
        <div class="chrome-bar">app.cadencehr.ai / workspace</div>
      </div>
      <div class="hero-mockup-frame">
        <img
          src={withBase('/screenshots/cadence-calendar-meetings-rendered-2026-07-10.webp')}
          alt="Cadence 1:1 Meetings workspace showing scheduled manager-report conversations"
          class="hero-mockup-shot"
          width="1440"
          height="1000"
          loading="eager"
          decoding="async"
        />
      </div>
    </div>
  </div>
</section>

<!-- PRODUCT PROOF -->
<section class="section section-white" id="proof">
  <div class="container">
    <div class="center">
      <div class="section-badge">Product Proof</div>
      <h2 class="section-headline fade-up">Real product surfaces, expandable for inspection.</h2>
      <p class="section-subhead fade-up">The public site uses rendered in-app views for 1:1s, goals, Personality, and 9-box calibration; live workflows stay separated from roadmap previews.</p>
    </div>
    <div class="proof-grid">
      {proofScreenshots.map((proof) => (
        <article class="proof-card fade-up">
          <a href={withBase(proof.image)} target="_blank" rel="noreferrer" aria-label={`Open ${proof.title} screenshot`}>
            <img src={withBase(proof.image)} alt={`${proof.title} product screenshot`} loading="lazy" decoding="async" />
          </a>
          <div class="proof-card-body">
            <h3>{proof.title}</h3>
            <p>{proof.text}</p>
          </div>
        </article>
      ))}
    </div>
    <p class="proof-note">Click any screenshot to expand the real asset in a new tab.</p>
  </div>
</section>

<!-- HUMAN-IN-THE-LOOP / TRUST -->
<section class="section section-light" id="how-it-works">
  <div class="container">
    <div class="center">
      <div class="section-badge">How Cadence Works</div>
      <h2 class="section-headline fade-up" style="max-width:680px;">AI develops managers. Managers develop people.</h2>
      <p class="section-subhead fade-up" style="max-width:640px;">Cadence is a management operating system — not a decision-maker. The AI helps managers prepare, reflect, and improve. Every people decision stays with the human who owns it.</p>
    </div>
    <div class="hitl-grid">
      <div class="hitl-card fade-up">
        <div class="hitl-icon hitl-icon-1">🧭</div>
        <h3>Human-in-the-loop</h3>
        <h4>AI coaches. You decide.</h4>
        <p>Cadence surfaces patterns — low 1:1 frequency, recognition activity, goal drift, ER context, survey signals, and consented working-style context where Personality Intelligence is enabled. Woven 1:1 prompts remain roadmap-labeled. Your managers review every suggestion, edit every agenda item, and make every call. The AI never takes an action on a person that isn't human-approved first.</p>
        <div class="hitl-quote">"The tool does the preparation. The manager does the leading."</div>
      </div>
      <div class="hitl-card fade-up" style="transition-delay:0.1s">
        <div class="hitl-icon hitl-icon-2">🔒</div>
        <h3>Data &amp; Privacy</h3>
        <h4>Your conversations stay yours.</h4>
        <p>What's said in a 1:1 stays governed by tenant access controls and consent policy. AI-generated summaries require the same consent guardrails as recording, and HR-facing views should rely on aggregate signals rather than raw meeting transcripts. Your workforce data never trains any AI model, ours or anyone else's.</p>
        <div class="hitl-quote">Audio capture is optional. The platform delivers full value with text-only structured notes.</div>
      </div>
      <div class="hitl-card fade-up" style="transition-delay:0.2s">
        <div class="hitl-icon hitl-icon-3">📋</div>
        <h3>Documentation</h3>
        <h4>The record your counsel will appreciate.</h4>
        <p>Consistent, timestamped 1:1 notes, goal updates, and ER case documentation built into every interaction — not added retroactively. When you need a defensible performance record, it's already there, structured and complete, from the first conversation.</p>
        <div class="hitl-quote">Built for the scenario where a manager and an employee disagree about what was said.</div>
      </div>
    </div>
    <div class="trust-strip fade-up">
      <div class="trust-pill"><span>🔒</span> Data encrypted at rest &amp; in transit</div>
      <div class="trust-pill"><span>👤</span> No AI decisions on people</div>
      <div class="trust-pill"><span>🚫</span> No customer-data model training by default</div>
      <div class="trust-pill"><span>📄</span> DPA available for review</div>
      <div class="trust-pill"><span>✅</span> SOC 2 readiness in progress</div>
      <div class="trust-pill"><span>🔍</span> <a href={withBase('/security')}>Full trust &amp; compliance details →</a></div>
    </div>
  </div>
</section>

<!-- PROBLEM -->
<section class="section-problem" id="problem">
  <div class="container">
    <div class="section-tag" style="color:rgba(255,255,255,0.92)">The Problem</div>
    <h2 class="section-headline white fade-up" style="max-width:560px;">The management problem hiding in plain sight.</h2>
    <div class="problem-grid">
      <div class="problem-text fade-up">
        <p>When your team is distributed, the informal mechanisms that made management work disappear. No hallway conversations. No reading body language. No overhearing who's struggling or celebrating who's winning in the break room.</p>
        <p>What you have instead: a stack of disconnected tools, compliance theater, and a manager population whose effectiveness varies wildly — and whose impact on retention you can't measure until someone resigns.</p>
      </div>
      <div class="failure-cards fade-up" style="transition-delay:0.1s">
        <div class="failure-card">
          <div class="failure-icon">🔗</div>
          <h4>The Fragmented Stack</h4>
          <p>One tool for 1:1s. Another for performance. Another for surveys. Another for recognition. Six platforms that don't talk to each other — and nobody has the full picture.</p>
        </div>
        <div class="failure-card">
          <div class="failure-icon">🗃️</div>
          <h4>The HRIS Gap</h4>
          <p>Your HRIS tracks records. It wasn't built for operational management. Forcing it into that role produces clunky workflows and zero intelligence.</p>
        </div>
        <div class="failure-card">
          <div class="failure-icon">🎲</div>
          <h4>The Lottery</h4>
          <p>Without a system, management quality is luck. Get a gifted coach and your career accelerates. Get an inconsistent manager and you become a resignation statistic.</p>
        </div>
      </div>
    </div>
    <div class="problem-bridge fade-up">
      <strong>Cadence</strong> is the system that closes all three gaps — and produces intelligence none of them can.
    </div>
  </div>
</section>

<!-- SOLUTION OVERVIEW -->
<section class="section section-white" id="solution">
  <div class="container">
    <div class="section-tag">The Solution</div>
      <h2 class="section-headline fade-up" style="max-width:640px;">One operating plane. Live management workflows, with roadmap depth clearly marked.</h2>
    <p class="section-subhead fade-up">Cadence sits above your HRIS and alongside your video calls — the workspace where management actually happens.</p>
    <div class="pillars">
      <div class="pillar fade-up">
        <div class="pillar-icon pillar-icon-1">✅</div>
        <h3>Consistency</h3>
        <h4>Systematic management</h4>
        <p>Structured 1:1s, goal tracking, and operating rhythms happen reliably — not when managers remember to schedule them.</p>
      </div>
      <div class="pillar fade-up" style="transition-delay:0.1s">
        <div class="pillar-icon pillar-icon-2">📈</div>
        <h3>Intelligence</h3>
        <h4>Unified intelligence</h4>
        <p>Connect 1:1 quality, recognition patterns, ER case history, survey sentiment, and consented working-style context into one Culture Scorecard no fragmented stack can produce. Predictive retention and employment-decision use stay off the current-claim list.</p>
      </div>
      <div class="pillar fade-up" style="transition-delay:0.2s">
        <div class="pillar-icon pillar-icon-3" aria-hidden="true"></div>
        <h3>Coaching</h3>
        <h4>AI that develops managers</h4>
        <p>AI summaries and private coaching are live development infrastructure. They raise the floor on management quality without replacing the relationship or making decisions about people.</p>
      </div>
    </div>
  </div>
</section>

<!-- PLATFORM MODULES PREVIEW -->
<section class="section section-light" id="modules">
  <div class="container">
    <div class="center">
      <div class="section-badge">Platform Modules</div>
      <h2 class="section-headline fade-up">Core management workflows live today. Roadmap modules are labeled.</h2>
      <p class="section-subhead fade-up">Cadence connects the operating rhythms managers already run. Preview capabilities are shown for direction, not sold as currently available.</p>
    </div>
    <div class="modules-grid">
      <a href={withBase('/product#one-on-ones')} class="module-tile fade-up">
        <span class="module-emoji">💬</span>
        <h3>1:1 Management Engine</h3>
        <p>The heartbeat of Cadence. Structured agendas, AI summaries, private coaching, and follow-through are live workflows.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#goals')} class="module-tile fade-up" style="transition-delay:0.05s">
        <span class="module-emoji">🎯</span>
        <h3>Goals &amp; OKR Tracking</h3>
        <p>Goal creation, updates, and progress tracking are live. Goal context inside the 1:1 is preview until backed by current tenant proof.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#personality-intelligence')} class="module-tile fade-up" style="transition-delay:0.1s">
        <span class="module-emoji">🧩</span>
        <h3>Personality Intelligence <span style="font-size:11px;color:var(--success);font-weight:800;text-transform:uppercase;">Live</span></h3>
        <p>Personality working-style profiles, team view, and coaching read-surface are live in Professional. Woven prompts, retention inputs, and any scoring or employment-decision use remain roadmap or never.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#nine-box')} class="module-tile fade-up" style="transition-delay:0.15s">
        <span class="module-emoji">⬛</span>
        <h3>9-Box Talent Calibration <span style="font-size:11px;color:var(--success);font-weight:800;text-transform:uppercase;">Live</span></h3>
        <p>Core 9-box calibration workflows are available in Professional and Enterprise. KVD, predictive retention, and advanced movement analysis stay roadmap-labeled.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#er')} class="module-tile fade-up" style="transition-delay:0.2s">
        <span class="module-emoji">📋</span>
        <h3>ER Command Center</h3>
        <p>Full case lifecycle from verbal warning to PIP. Connected to the full management record — so context exists before the case opens.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#recognition')} class="module-tile fade-up" style="transition-delay:0.25s">
        <span class="module-emoji">⭐</span>
        <h3>Recognition</h3>
        <p>Recognition feed and posting are live. Recognition-ratio analysis is preview, not an enforced GA control.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#surveys')} class="module-tile fade-up" style="transition-delay:0.3s">
        <span class="module-emoji">📊</span>
        <h3>Survey Engine</h3>
        <p>Pulse response collection and aggregate survey analytics are live. Full campaign templates and manager-level attribution remain preview or roadmap until verified.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#job-architecture')} class="module-tile fade-up" style="transition-delay:0.35s">
        <span class="module-emoji">🏗️</span>
        <h3>Job Architecture</h3>
        <p>Job architecture import, versioned role definitions, level context, and role assignments support admin setup today. Role-recasting workflows remain roadmap.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#connected-agents')} class="module-tile fade-up" style="transition-delay:0.38s">
        <span class="module-emoji">🔌</span>
        <h3>Connected Agents (BYOA) <span style="font-size:11px;color:var(--accent);font-weight:800;text-transform:uppercase;">Preview</span></h3>
        <p>BYOA over MCP is preview-labeled. External customer connections open after scope-consent and scoped audit logging ship.</p>
        <span class="module-link">Learn more →</span>
      </a>
      <a href={withBase('/product#culture-scorecard')} class="module-tile fade-up" style="transition-delay:0.4s">
        <span class="module-emoji">🏆</span>
        <h3>Culture Scorecard</h3>
        <p>The unified intelligence layer. Manager scorecard and org-health heatmap views are live; predictive turnover, board-ready reporting, and scoring remain roadmap items.</p>
        <span class="module-link">Learn more →</span>
      </a>
    </div>
    <div style="text-align:center;margin-top:40px;" class="fade-up">
      <a href={withBase('/product')} class="btn btn-primary btn-lg">Explore the full platform →</a>
    </div>
  </div>
</section>

<!-- WHO IT'S FOR -->
<section class="section section-white" id="personas">
  <div class="container">
    <div class="center">
      <div class="section-badge">Built For Everyone</div>
      <h2 class="section-headline fade-up">Built for every layer of the org — one platform.</h2>
      <p class="section-subhead fade-up">Cadence looks different depending on where you sit: IC clarity, manager team health, L2 chain signal, CHRO command, and CEO altitude.</p>
    </div>
    <div class="personas-grid">
      <a href={withBase('/solutions#individual-contributors')} class="persona-card persona-ic fade-up">
        <div class="persona-avatar">👤</div>
        <h3>Individual Contributors</h3>
        <p>Your 1:1s, goals, recognition history, and private coaching in one system.</p>
      </a>
      <a href={withBase('/solutions#managers')} class="persona-card persona-mgr fade-up" style="transition-delay:0.1s">
        <div class="persona-avatar">🧑‍💼</div>
        <h3>Managers</h3>
        <p>The workspace you actually run 1:1s in. Structured follow-through, AI coaching, and core 9-box calibration are live; recognition-ratio enforcement remains preview.</p>
      </a>
      <a href={withBase('/solutions#senior-leaders')} class="persona-card persona-l2 fade-up" style="transition-delay:0.2s">
        <div class="persona-avatar">📡</div>
        <h3>Senior Leaders (L2+)</h3>
        <p>Manager-chain visibility across 1:1 activity, ER context, survey sentiment, and org-health signals. Recognition-ratio enforcement remains preview.</p>
      </a>
      <a href={withBase('/solutions#chro')} class="persona-card persona-chro fade-up" style="transition-delay:0.3s">
        <div class="persona-avatar">🏛️</div>
        <h3>CHROs &amp; People Leaders</h3>
        <p>Operational command center — ER pipeline, active PIPs, survey campaigns, manager scorecard and org-health heatmap views, and calibration cycle progress.</p>
      </a>
      <a href={withBase('/solutions#ceo')} class="persona-card persona-ceo fade-up" style="transition-delay:0.4s">
        <div class="persona-avatar">📈</div>
        <h3>CEOs</h3>
        <p>Strategic altitude on whether the organization is healthy enough to execute: Culture Scorecard rollups, hotspots, and focused drill-down.</p>
      </a>
    </div>
    <div style="text-align:center;margin-top:40px;" class="fade-up">
      <a href={withBase('/solutions')} class="btn btn-secondary btn-lg">See solutions by role →</a>
    </div>
  </div>
</section>

<!-- PRICING PREVIEW -->
<section class="section section-light" id="pricing">
  <div class="container">
    <div class="center">
      <div class="section-badge">Transparent Pricing</div>
      <h2 class="section-headline fade-up">Consolidate the management workflows that are live today.</h2>
      <p class="section-subhead fade-up">Pricing starts at $12/seat/month. Self-serve access is live, and roadmap capabilities are labeled separately from current deliverables.</p>
    </div>
    <div class="pricing-grid">
      <div class="pricing-card fade-up">
        <div class="pricing-tier">Essentials</div>
        <div class="pricing-price">$12<span>/seat/mo</span></div>
        <div class="pricing-meta">GA target · Billed annually · Min. 50 seats</div>
        <div class="pricing-desc">Core management operating plane for organizations building their management foundation.</div>
        <ul class="pricing-features">
          <li>1:1 Management Engine</li>
          <li>Goals &amp; OKR Tracking</li>
          <li>Recognition &amp; Rewards</li>
          <li>Pulse survey response collection</li>
          <li>Job Architecture</li>
          <li>AI meeting summaries</li>
        </ul>
        <a href="https://app.cadencehr.ai/signup?tier=essentials&intent=self-serve&source=apex-home-pricing" class="btn-pricing btn-pricing-outline">Start free trial</a>
      </div>
      <div class="pricing-card featured fade-up" style="transition-delay:0.1s">
        <div class="pricing-badge">Most Popular</div>
        <div class="pricing-tier">Professional</div>
        <div class="pricing-price">$24<span>/seat/mo</span></div>
        <div class="pricing-meta">GA target · Billed annually · Min. 100 seats</div>
        <div class="pricing-desc">Everything in Essentials, plus the live People-ops workflows available in current purchasable access. Roadmap modules stay labeled separately.</div>
        <ul class="pricing-features">
          <li>Everything in Essentials</li>
          <li>9-Box Talent Calibration</li>
          <li>Personality profile, team view, and coaching read-surface</li>
          <li>ER Command Center</li>
          <li>Manager scorecard and org-health heatmap</li>
          <li>Roadmap preview: Turnover prediction</li>
          <li>Preview: advanced integrations</li>
        </ul>
        <a href="https://app.cadencehr.ai/signup?tier=professional&intent=self-serve&source=apex-home-pricing" class="btn-pricing btn-pricing-primary">Start trial</a>
      </div>
      <div class="pricing-card fade-up" style="transition-delay:0.2s">
        <div class="pricing-tier">Enterprise</div>
        <div class="pricing-price" style="font-size:32px;">Custom</div>
        <div class="pricing-meta">500+ seats · Contact sales</div>
        <div class="pricing-desc">Full platform with enterprise support and roadmap security options clearly scoped during procurement.</div>
        <ul class="pricing-features">
          <li>Everything in Professional</li>
          <li>Enterprise SSO (OIDC)</li>
          <li>Roadmap: SAML SSO in Q4</li>
          <li>Roadmap: SCIM provisioning</li>
          <li>Roadmap: custom HRIS integrations</li>
          <li>Configurable data retention</li>
          <li>Dedicated customer success</li>
          <li>Contract-dependent GA target: SLA guarantees</li>
        </ul>
        <a href="mailto:demo@cadencehr.ai?subject=Enterprise%20pricing" class="btn-pricing btn-pricing-outline">Talk to us</a>
      </div>
    </div>
    <div style="text-align:center;margin-top:36px;" class="fade-up">
      <a href={withBase('/pricing')} class="btn btn-secondary btn-lg">See full feature comparison →</a>
    </div>
    <p style="text-align:center;margin-top:16px;font-size:12px;color:var(--gray-500);">Self-serve access starts through the live signup funnel; Enterprise remains sales-led.</p>
  </div>
</section>

<!-- CTA -->
<section class="section-cta">
  <div class="container">
    <div style="font-size:12px;font-weight:700;color:rgba(255,255,255,0.92);text-transform:uppercase;letter-spacing:1px;margin-bottom:14px;">Get Started</div>
    <h2>Stop leaving management quality to chance.</h2>
    <p>Every day without a system is another day where your employees' careers depend on whether they got lucky with their manager. Cadence is the operating plane that changes that.</p>
    <div class="btn-group" style="justify-content:center;">
      <a href="https://app.cadencehr.ai/signup?intent=self-serve&source=apex-home-cta" class="btn btn-white btn-lg">Start free trial</a>
      <a href={withBase('/product')} class="btn btn-white-outline btn-lg">Explore the platform</a>
    </div>
  </div>
</section>

</BaseLayout>
